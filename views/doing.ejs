<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NewDoing</title>
</head>
<body>
    <div class="container">
        
        <div class="container">
         <h3><%= heading %></h3>
          <% if (locals.doing){ %>
            
          <form id="editfrom" method="post" action="/api/doing/<%= doing.id %>">
            <input type="text" name="title" value="<%=doing.title %>" />
            <textarea id="content" name="content" rows="10"><%=doing.content %></textarea>
            <button class="full-width" type="submit"><%= submit %></button>
          </form>
    
          <% } else { %>


        <form id="newTask" method="post" action="/api/doing">
       
            <input type="text" name="title" placeholder="Title" required />
            <textarea name="content" id="escription" required rows="10"></textarea>
            <button type="submit"><%= submit %></button>
            <% } %>
          </form>
    
    </div>
</body>
</html>