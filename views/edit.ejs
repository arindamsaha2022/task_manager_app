<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit tasks</title>
  </head>
  <body>
    <h1>creat your new task</h1>
    <h3><%= heading %></h3>
    <div class="container">
      <% if (locals.task){ %>
      <form id="editfrom" method="post" action="/api/tasks/<%= task.id %>">        
        <input type="text" name="title" value="<%=task.title %>" />
        <textarea id="content" name="content" rows="10"><%=task.content %></textarea>
        <button class="full-width" type="submit"><%= submit %></button>
      </form>

      <% } else { %>

      <form id="newTask" method="post" action="api/tasks">
        <input type="text" name="title" placeholder="Title" required />
        <textarea name="content" id="description" required rows="10"></textarea>
        <button type="submit"><%= submit %></button>
      </form>
      <% } %>
    </div>
  </body>
</html>
